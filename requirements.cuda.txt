# RAPIDS packages with CUDA 13 support (25.12 stable release)
# cudf includes built-in Polars interoperability (df.to_polars() / cudf.from_polars())
--extra-index-url=https://pypi.nvidia.com
cudf-cu13==25.12.*


# PyTorch with CUDA 13 (from PyTorch index)
--extra-index-url=https://download.pytorch.org/whl/cu130
torch
torchvision
torchaudio

# JupyterLab and extensions
jupyterlab
jupyterlab-git
jupyter-resource-usage
# Pinned to upstream main for the www-root-path double-slash fix (issue #41);
# revert to pinned release once a version > 2.5.0 is on PyPI.
jupyter-rsession-proxy @ git+https://github.com/jupyterhub/jupyter-rsession-proxy.git@main
jupyter-vscode-proxy
jupyterhub
jupyter-tensorboard-proxy
jupyter-keepalive

# Data analysis packages
altair
duckdb
duckdb-engine
ibis-framework[duckdb]
polars
plotnine
pypdf

# Development and utilities
gh-scoped-creds
glances
git-filter-repo
